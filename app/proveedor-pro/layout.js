import "./tailwind.css";

export const metadata = {
  title: "Proveedor Pro · Drokex",
  description: "Vende directamente en Drokex con tu propia tienda, stock local y gestión completa.",
};

export default function ProveedorProLayout({ children }) {
  return children;
}
